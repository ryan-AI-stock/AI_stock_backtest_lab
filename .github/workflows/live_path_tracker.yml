name: Live Path Tracker Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled by product decision
        run: |
          echo "This workflow is intentionally disabled."
          echo "The live path tracking report is no longer generated or published."
          echo "Existing Google Drive files are not deleted by this workflow."
